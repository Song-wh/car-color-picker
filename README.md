# 🎨 CarColor - 자동차 조색 앱

자동차 도색 색상을 분석하고 페인트 코드를 찾아주는 모바일 앱입니다.

## ✨ 주요 기능

### 📷 실시간 카메라 색상 추출
- 카메라로 차량을 비추면 실시간으로 색상 정보 표시
- 중앙 포인트의 색상을 추출하여 RGB/HEX/HSL/LAB 값 제공
- 터치하여 원하는 부분의 색상 캡처

### 🤖 AI 차량 분석
- OpenAI Vision API를 활용한 차량 인식
- 제조사, 모델, 연식, 차종 자동 분석
- 색상 타입 (솔리드/메탈릭/펄) 분석

### 🎯 페인트 코드 매칭
- 캡처한 색상과 가장 유사한 페인트 코드 추천
- 현대, 기아, BMW, 벤츠 등 주요 제조사 페인트 코드 DB
- Delta E 알고리즘으로 정확한 색상 유사도 계산

### 📋 색상 정보 복사
- HEX, RGB, HSL, LAB 값 원클릭 복사
- 조색 작업에 바로 활용 가능

## 🚀 시작하기

### 설치

```bash
cd car-color-picker
npm install
```

### 개발 서버 실행

```bash
npm run dev
```

### 안드로이드 빌드

```bash
npm run build
npx cap add android
npx cap sync
npx cap open android
```

## ⚙️ 설정

### OpenAI API 키 설정

AI 차량 분석 기능을 사용하려면 OpenAI API 키가 필요합니다.

1. [OpenAI Platform](https://platform.openai.com/api-keys)에서 API 키 발급
2. 앱 우측 상단 설정 버튼 클릭
3. API 키 입력 후 저장

## 📱 사용 팁

1. **🌤️ 자연광 촬영**: 자연광에서 촬영하면 더 정확한 색상 추출
2. **📐 수직 촬영**: 차량과 수직으로 촬영
3. **🎯 깨끗한 부분**: 먼지나 오염이 없는 깨끗한 부분 촬영
4. **💡 그림자 피하기**: 그림자가 없는 곳에서 촬영
5. **🔄 다중 촬영**: 여러 각도에서 촬영해 비교

## 🔧 기술 스택

- **Frontend**: React 18 + Vite
- **Mobile**: Capacitor (iOS/Android)
- **AI**: OpenAI GPT-4 Vision API
- **색상 처리**: CIE LAB 색공간, Delta E 알고리즘
- **UI**: Lucide React Icons

## 📁 프로젝트 구조

```
car-color-picker/
├── src/
│   ├── components/
│   │   ├── CameraView.jsx      # 카메라 뷰 & 색상 추출
│   │   ├── ColorResult.jsx     # 색상 결과 표시
│   │   ├── CarInfo.jsx         # AI 차량 정보
│   │   └── Settings.jsx        # 설정 모달
│   ├── data/
│   │   └── paintCodes.js       # 페인트 코드 데이터베이스
│   ├── services/
│   │   └── aiService.js        # OpenAI API 연동
│   ├── utils/
│   │   └── colorUtils.js       # 색상 변환 유틸리티
│   ├── App.jsx
│   ├── App.css
│   └── main.jsx
├── capacitor.config.json
└── package.json
```

## 🎨 페인트 코드 DB 확장

`src/data/paintCodes.js`에서 페인트 코드를 추가할 수 있습니다:

```javascript
export const paintCodes = {
  '브랜드명': [
    { code: 'ABC', name: '색상명', hex: '#FFFFFF', rgb: [255, 255, 255] },
    // ...
  ],
};
```

## 📄 라이선스

MIT License
